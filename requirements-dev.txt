# Development / CI dependencies for the whole CogniOps repository.
# Aggregates runtime-, design-, security-agent, evaluation, baseline and
# cloud-function test dependencies. For production deployments each service
# installs only its own requirements.txt.

# Root / shared
-r requirements.txt

# Runtime agent
-r runtime-agent/requirements.txt

# Design-time agent
-r design-agent/requirements.txt

# Security compliance agent
-r security-agent/requirements.txt

# Evaluation framework
-r evaluation/requirements.txt

# Lint / format (used by Justfile and pre-commit fallback)
ruff>=0.5.0,<0.6.0
pre-commit>=3.7,<4.0

# Baseline services (tests)
opencv-python-headless>=4.9,<5.0

# Cloud Functions (tests)
-r functions/ingest_runs/requirements.txt
